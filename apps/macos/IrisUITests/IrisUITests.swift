import XCTest

final class IrisUITests: XCTestCase {
    func testMenuBarShellLaunches() throws {
        throw XCTSkip(
            "UI smoke coverage starts in Step 3.1 when the menu bar shell renders real state."
        )
    }
}
